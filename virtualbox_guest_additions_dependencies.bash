sudo apt update && \
    sudo apt upgrade && \
    sudo apt dist-upgrade && \
    sudo apt autoremove && \
    sudo apt install \
        gcc \
        make \
        perl \
        build-essential \
        dkms
